# Copy to .env.local and fill in. .env.local is git-ignored — never commit keys.

# Optional: pin the demo dataset to a fixed date (YYYY-MM-DD) at build time.
# Prefer ?asof=YYYY-MM-DD on the URL — it needs no rebuild.
VITE_DEMO_ANCHOR=
