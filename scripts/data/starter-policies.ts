/**
 * Re-export: the starter policies moved beside the admin edge function so
 * the UI-driven onboarding and this CLI share one source of truth.
 */
export { STARTER_POLICIES, type StarterPolicy } from '../../supabase/functions/admin/starter-policies'
